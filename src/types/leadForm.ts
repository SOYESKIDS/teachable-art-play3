/** B2B Lead 접수 Form 전용 타입 (lead_submissions 테이블과 대응) */

export type SubmissionType = "pilot" | "demo" | "consult" | "purchase_interest";

export type PackageCode = "starter" | "standard" | "premium" | "undecided";
